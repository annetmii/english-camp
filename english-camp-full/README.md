# annetmii English Camp (no-build版)

## 使い方
1. このフォルダを GitHub の `annetmii/english-camp` 直下にアップロード
2. Netlify の Publish directory を `.` に、Functions を `netlify/functions` に設定
3. Netlify の環境変数 `GITHUB_TOKEN` を追加（対象リポの Contents: Read & Write）
4. 公開URLにアクセス → 講師モードでPIN設定 → テーマ入力 → 送信でクラウド保存
