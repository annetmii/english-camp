{
  "name": "annetmii-english-camp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "deploy:prod": "vite build && npx netlify deploy --prod --dir=dist --functions=netlify/functions",
  "deploy:front": "vite build && npx netlify deploy --prod --dir=dist"
},
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@netlify/functions": "^2.7.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.0"
  }
}
